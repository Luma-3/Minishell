# defined to 1 if subunit is enabled
ENABLE_SUBUNIT=0
export ENABLE_SUBUNIT
EXEEXT=
export EXEEXT
HAVE_FORK=1
export HAVE_FORK

# path of the tests directory
if [ x"/home/jbrousse/Downloads/check-0.15.2/tests" != x"." ]; then
    if [ -z "" -o "" != "1" ]; then
       SRCDIR="/home/jbrousse/Downloads/check-0.15.2/tests/"
    else
       SRCDIR="/home/jbrousse/Downloads/check-0.15.2/tests\\"
    fi
else
    SRCDIR=""
fi

export SRCDIR
